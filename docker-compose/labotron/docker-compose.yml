services:
  # oracle-db:
  #   #    container_name: oracle-db
  #   image: gvenzl/oracle-xe:latest
  #   environment:
  #     - ORACLE_PASSWORD=12345678
  #     - ORACLE_DATABASE=labotron
  #     - APP_USER=labotron
  #     - APP_USER_PASSWORD=123456
  #   ports:
  #     - "${ORACLE_PORT}:1521"
  #     #- "8585:8080"
  #   volumes:
  #     - ./oracle_data:/opt/oracle/oradata
  #   healthcheck:
  #     test: [ "CMD-SHELL", "healthcheck.sh" ]
  #     interval: 50s
  #     timeout: 10s
  #     retries: 5

#   labotronservice:
#     image: ${LABOTRON_SERVICE_IMAGE}
# #    container_name: labotronservice  # Explicit name
#     ports:
#       - "${LABOTRON_SERVICE_PORT}:8080"
#     environment:
# #      - SPRING_PROFILES_ACTIVE=prod   # Spring profile
#       - JAVA_OPTS=-Xmx4092m -Xms256m   # JVM memory limits
#     restart: unless-stopped
#     volumes:
#       - ./logs:/usr/local/tomcat/logs # Persist logs
#     healthcheck:
#       test: ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
#       interval: 30s
#       timeout: 10s
#       retries: 3

#   # Flutter Web Frontend
#   labotronweb:
#     image: ${LABOTRON_WEB_IMAGE}
# #    container_name: labotron-web
#     ports:
#       - "${LABOTRON_WEB_PORT}:80"  # Frontend on standard HTTP port
#     environment:
#       - API_BASE_URL=http://labotron-api:8080  # Internal network URL
#     restart: always

  rabbitmq:
    image: rabbitmq:3-management
    restart: always
    ports:
      - "${RABBITMQ_MAIN_PORT}:5672"
      - "${RABBITMQ_MGMT_PORT}:15672"
    # environment:
    #   RABBITMQ_ERLANG_COOKIE: "***********************"
    #   RABBITMQ_DEFAULT_USER: "guest"
    #   RABBITMQ_DEFAULT_PASS: "guest"
    #   RABBITMQ_DEFAULT_VHOST: "/"
    volumes:
      - ./rabbitmq/data:/var/lib/rabbitmq
      # - ./rabbitmq/etc:/etc/rabbitmq

